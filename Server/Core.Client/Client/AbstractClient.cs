using System;
using System.Collections.Generic;
using System.Data.Common;
using Core.Common;

namespace Core.Client
{
    public class AbstractClient
    {
        //private Connector<AbstractConnection> m_connector;
        //private AbstractPacketResolver<Connector<AbstractConnection>> _packetResolver;

        //public AbstractClient()
        //{
        //}

        //public virtual void Initialize(string configPath)
        //{
        //    ClientConfig.Instance.Load(configPath);

        //    m_connector = new Connector<AbstractConnection>();
        //    m_connector.Initialize(ClientConfig.Instance.ReceiveBufferSize);

        //    _packetResolver = OnGetPacketResolver();
        //}

        //protected abstract AbstractPacketResolver<Connector<AbstractConnection>> OnGetPacketResolver();
    }
}
